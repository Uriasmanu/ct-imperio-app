workflows:
  expo-classic-build:
    name: Expo Classic Build Android AAB
    max_build_duration: 120
    instance_type: mac_mini_m2

    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Login Expo
        script: |
          npx expo login -u maanoelaurias@gmail.com -p /thYAH?$H+S7Q9r
      - name: Build Android AAB
        script: |
          npx expo build:android -t app-bundle
